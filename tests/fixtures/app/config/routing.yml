homepage:
    pattern: /
    defaults: { _controller: "DemoBundle:Default:Index" }
    
redirect:
    pattern: /redirect
    defaults: { _controller: "DemoBundle:Default:Redirect" }
    
error_page:
    pattern: /error_page
    defaults: { _controller: "DemoBundle:Default:Error" }